version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/sqlc/queries.sql"
    schema: "./db/migrations"
    gen:
      go:
        package: "repo"
        out: "./db/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true
